{
  "name": "@repo/core",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'no build step'",
    "check": "biome check .",
    "types": "tsc --noEmit",
    "test": "vitest run",
    "clean": "echo 'no clean step'"
  },
  "dependencies": {
    "chordsheetjs": "^14.2.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
